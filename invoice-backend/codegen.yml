schema: "./src/GraphQL/typeDefs.ts" # Path to your typeDefs
documents: "./tests/**/*.test.ts" # Path to your test files with GraphQL queries
watch: true
generates:
  ./src/generated/graphql.ts:
    plugins:
    - "typescript"
    - "typescript-operations"
    config:
      useTypeImports: true
